export type Profile = {
  id: string
  display_name: string
  created_at: string
  updated_at: string
}

export type ProfileActivity = {
  reviewsWritten: number
  facilitiesContributed: number
  placesVerified: number
}
